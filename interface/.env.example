# rtgf-interface — Telegram Bot
# Copy to: interface/.env (never commit)

# Telegram bot token from @BotFather
TELEGRAM_TOKEN=7843920183:AAF...

# Your Telegram user ID (get it by messaging the bot /whoami after first run)
# Used to gate admin commands
ADMIN_CHAT_ID=123456789

# LiteLLM gateway URL (running on Ubuntu-AI-Hub or localhost)
GATEWAY_URL=http://localhost:4000

# LiteLLM master key (for /spend and /models admin commands)
LITELLM_MASTER_KEY=sk-master-...

# Default virtual key if no chat-specific key configured
# Run: ./gateway/setup-client.sh personal
LITELLM_DEFAULT_KEY=sk-virt-...

# LORE archive root for session search
LORE_ARCHIVE=/home/cbasta/rtgf-ai-stack/lore/ctx/archive/canonical
