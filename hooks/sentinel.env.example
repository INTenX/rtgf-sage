# SENTINEL Hook Configuration
# Copy to ~/.claude/hooks/sentinel.env and fill in values.
# This file should NOT be committed to the repo.
#
# Both values are required for Telegram alerting.
# Hooks still function (log-only) without these set.

# Telegram bot token — from @BotFather
TELEGRAM_TOKEN=

# Chat ID to receive SENTINEL block alerts
# Use your personal chat ID or a dedicated #sentinel-alerts group
TELEGRAM_CHAT_ID=
