# LiteLLM Gateway — Docker Compose
# INTenX AI Stack — compose/gateway.yml
#
# Start:   docker compose -f compose/gateway.yml --env-file gateway/.env up -d
# Stop:    docker compose -f compose/gateway.yml down
# Logs:    docker compose -f compose/gateway.yml logs -f litellm
# Health:  curl http://localhost:4000/health
#
# Env:     Copy gateway/.env.example to gateway/.env and fill in values

name: litellm

services:

  litellm:
    image: ghcr.io/berriai/litellm:main-stable
    container_name: litellm-gateway
    restart: unless-stopped
    ports:
      - "4000:4000"
    volumes:
      - ../gateway/config.yaml:/app/config.yaml:ro
    environment:
      LITELLM_CONFIG_PATH: /app/config.yaml
      LITELLM_MASTER_KEY: ${LITELLM_MASTER_KEY}
      # Hub: DATABASE_URL=postgresql://litellm:litellm@db:5432/litellm
      # Dev (SQLite fallback): DATABASE_URL=sqlite:////data/litellm.db
      DATABASE_URL: ${DATABASE_URL}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OLLAMA_API_BASE: ${OLLAMA_API_BASE:-http://host.docker.internal:11434}

    command: >
      --config /app/config.yaml
      --port 4000
      --host 0.0.0.0

    depends_on:
      db:
        condition: service_healthy

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4000/health/liveliness"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

    deploy:
      resources:
        limits:
          memory: 512m

  db:
    image: postgres:16-alpine
    container_name: litellm-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-litellm}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-litellm}
      POSTGRES_DB: ${POSTGRES_DB:-litellm}
    volumes:
      - litellm-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-litellm}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  litellm-db-data:
    name: litellm-db-data
    external: true
